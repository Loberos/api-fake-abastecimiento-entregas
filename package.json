{
  "name": "api-fake-abastecimiento-entregas",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}